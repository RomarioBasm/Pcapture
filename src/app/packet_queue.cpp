#include "app/packet_queue.hpp"

// All members are templated and defined inline in the header. This translation
// unit exists so the build system has a tangible compilation target for the
// pipeline component and so future, non-templated helpers can be added here.
